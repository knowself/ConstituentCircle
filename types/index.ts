// Update your UserProfile interface to match the actual structure
interface UserProfile {
  id: string;
  email: string;
  avatar_url?: string; // Add this property directly on the user object
  // Add other properties as needed
}